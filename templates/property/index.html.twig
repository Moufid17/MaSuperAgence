{% extends 'base.html.twig' %}

{% block title 'Estate' %}

{% block body %}
    <div class="jumbotron">
        {{ form_start(form) }}
            <div class="container">
                <div class="form-row align-items-end">
                    <div class="col-md-2"> {{ form_row(form.roomsMin) }}</div>
                    <div class="col-md-2"> {{ form_row(form.surfaceMin) }}</div>
                    <div class="col-md-2"> {{ form_row(form.priceMax) }}</div>
                    <div class="col-md-4"> {{ form_row(form.options) }}</div>
                    <div class="col-md-2">
                        <div class="form-group">
                            {# {{ form_row(form.Rechercher) }} #}
                            <button type="submit" class="btn btn-primary" style="height:38px">Rechercher</button>
                        </div>
                    </div>
                </div>
            </div>
        {{ form_end(form) }}
    </div>
    <div class="container mt-4">
        <h1>Tous nos bien</h1>
        <div class="row">
            {% for property in properties %}
                <div class="col-md-4">
                    {% include('property/_property.html.twig') %}
                </div>
            {% endfor %}
        </div>
    {{ knp_pagination_render(properties) }}
    </div>
{% endblock %}